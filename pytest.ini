[pytest]
markers =
    e2e: end-to-end pipeline tests that invoke scripts/run_all.sh (slow; opt-in)
# By default exclude the e2e mark so `pytest` stays fast (~3-5 min). Run the
# e2e suite explicitly via `pytest -m e2e` or `pytest tests/test_e2e.py`.
addopts = -m "not e2e"
